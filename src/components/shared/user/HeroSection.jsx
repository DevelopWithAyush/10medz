import React from 'react'
import UploadPrescription from '../../ui/user/UploadPrescription'

const HeroSection = () => {
  return (
      <section className='container mx-auto max-w-[88rem] px-[48px] '>
          <UploadPrescription className={"px-[0px] w-[360px]"} />
    </section>
  )
}

export default HeroSection
